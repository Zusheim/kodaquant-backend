# --- Web framework (bounds mirror what gradio==6.22.0 itself requires,
# since HF's Gradio SDK auto-installs it alongside this file) -------------
fastapi>=0.115.2,<1.0
starlette>=1.0.1,<2.0
uvicorn>=0.32.0,<1.0
uvloop==0.22.1
httptools==0.8.0
watchfiles==1.2.0
websockets==15.0.1
h11==0.16.0
h2==4.3.0
hpack==4.2.0
hyperframe==6.1.0
anyio>=4.14.1,<5.0

# --- Data validation / settings -------------------------------------------
# Range narrowed to what gradio[mcp]==6.22.0 itself requires
# (pydantic>=2.11.10,<=2.12.5) — ZeroGPU pulls in the mcp extra, whose
# pydantic bound is tighter than gradio's own base <=3.0.
pydantic>=2.11.10,<=2.12.5
# pydantic_core intentionally NOT pinned here: it must match pydantic's
# own exact internal requirement. Pinning both and then loosening only
# pydantic re-creates the same class of conflict this file just fixed.
pydantic-settings==2.14.2
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions>=4.16.0,<5.0
email-validator==2.3.0

# --- HTTP / networking -----------------------------------------------------
httpx>=0.28.1,<1.0
httpcore==1.0.9
requests==2.34.2
urllib3==2.7.0
certifi==2026.6.17
idna==3.18
charset-normalizer==3.4.7
dnspython==2.8.0
yarl==1.24.2
multidict==6.7.1
propcache==0.5.2
frozendict==2.4.7

# --- Auth / security ---------------------------------------------------
PyJWT==2.13.0
cryptography==48.0.1
cffi==2.0.0
pycparser==3.0

# --- Pagos ---------------------------------------------------------------
stripe>=15.0,<16.0

# --- ZeroGPU probe (gradio.Server nativo, ver main.py) -------------------
# `spaces` NO se pinea acá a propósito: la plataforma de HF Spaces ya lo
# fuerza a su propia versión en el build (ver el log de build: agrega
# "spaces==X.Y.Z" al pip install además de este archivo). Pinearlo de
# nuevo acá es un conflicto latente esperando pasar — ver la nota vieja
# más abajo, que documentaba el mismo riesgo sin eliminarlo.
#
# CRÍTICO: este pin (6.22.0) DEBE coincidir exacto con `sdk_version` en
# el front-matter YAML de README.md. HF inyecta `gradio[oauth,mcp]==<sdk_version>`
# en el MISMO comando pip que procesa este archivo — si divergen, pip recibe
# dos pines `==` mutuamente excluyentes del mismo paquete y falla con
# ResolutionImpossible de inmediato (no es un conflicto de grafo real, es
# un doble pin). Root cause del build fallido del 2026-08-12 (commit 6dcbbd6):
# no existía README.md, así que HF usaba su default de plataforma (6.23.1)
# mientras este archivo fijaba 6.22.0.
gradio[oauth,mcp]==6.22.0

# --- Database / Supabase -------------------------------------------------
supabase==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
postgrest==2.31.0
realtime==2.31.0
storage3==2.31.0
peewee==4.1.1

# --- Data / ML / forecasting (fixed — critical for the predictive models,
# not touched by the Gradio conflict) ---------------------------------
numpy==2.5.1
pandas==3.0.3
scikit-learn==1.6.1
keras==3.15.1
tensorflow>=2.16.1,<2.21
yfinance==0.2.44
multitasking==0.0.13
# curl_cffi: NO es dependencia declarada de yfinance==0.2.44 (verificado
# contra el METADATA del wheel) -- hay que pinearlo aparte. Sin esto,
# quanti_engine.py degrada solo a requests.Session estándar (huella TLS
# bloqueable por Cloudflare/Yahoo, ver _build_yf_session). Bound abierto
# a mayor de patch porque libera ABI compilado por SO/arch (manylinux) que
# HF Spaces resuelve en build; <1.0 evita saltos de API mayor sin probar.
curl_cffi>=0.7.1,<1.0

# --- LLM / search --------------------------------------------------------
groq>=0.31.0,<2.0.0
ddgs>=9.0.0

# NOTA: torch/transformers (FinBERT) NO se instalan a propósito --
# transformers exige huggingface-hub<1.0, gradio==6.22.0 exige
# huggingface-hub>=1.16.0: conflicto real, ninguna versión de transformers
# lo resuelve. `_score_text` en data_pipeline.py ya contempla esto -- ante
# ImportError cae a NEUTRAL_SCORE por titular (no rompe el radar, solo
# resigna el matiz de sentimiento de noticias). No agregar torch/transformers
# de nuevo sin antes resolver ese conflicto de raíz (p.ej. bajar gradio).

# --- Parsing / scraping ---------------------------------------------------
beautifulsoup4==4.15.0
lxml==6.1.1
html5lib==1.1
soupsieve==2.8.4
webencodings==0.5.1

# --- Email -----------------------------------------------------------------
aiosmtplib>=3.0.0

# --- Misc / packaging -------------------------------------------------
python-dotenv==1.2.2
python-dateutil==2.9.0.post0
pytz==2026.2
PyYAML>=6.0.3,<7.0
click==8.4.2
deprecation==2.1.0
packaging==26.2
platformdirs==4.10.0
setuptools==83.0.0
six==1.17.0
StrEnum==0.4.15
wheel==0.47.0